# Before vs. After Fine-Tuning

**Status:** Awaiting a trained LoRA adapter.

Use the same evaluation prompts with the base FLAN-T5 model and the LoRA-adapted model. Record outputs side by side and do not add performance claims until the evaluation has been run.
