# Before-vs-After Fine-Tuning Examples

This file intentionally contains no invented model output.

After training, generate the same held-out prompts with:

1. `python scripts/evaluate_model.py --base-only`
2. `python scripts/evaluate_model.py` with `ADAPTER_MODEL_ID` configured

Add representative examples only after reviewing the actual outputs.
