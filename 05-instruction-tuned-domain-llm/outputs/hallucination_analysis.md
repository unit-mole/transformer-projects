# Hallucination Analysis Status

**Status:** Not run.

Run the adapter evaluation first, then execute:

```bash
python scripts/run_hallucination_analysis.py
```

Review definitions, numeric claims, code samples, unsupported attributions, overconfident language, missing caveats, and any answer outside the intended ML/Data Science scope. Automated flags are only a triage aid; a human reviewer must make the final judgment.
