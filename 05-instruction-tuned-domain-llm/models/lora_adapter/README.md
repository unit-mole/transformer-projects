# Generated LoRA Adapter Directory

This folder is intentionally empty in the source bundle. Run:

```bash
python scripts/train_lora.py
```

The PEFT adapter, tokenizer files, and `training_metadata.json` will be saved here. Do not claim that an adapter was trained until the command completes successfully and the resulting metrics are reviewed.
