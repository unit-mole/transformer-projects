# Tokenizer Directory

The quality preset loads the tokenizer from `google/flan-t5-base`. A tokenizer saved during training is promoted here for local archival and deployment consistency. Keep the tokenizer aligned with the base model and LoRA adapter.
