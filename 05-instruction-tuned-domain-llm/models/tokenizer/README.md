# Tokenizer Artifacts

The project loads the tokenizer from `google/flan-t5-small`. Training also saves a tokenizer copy beside the LoRA adapter for reproducibility. Large generated artifacts should be uploaded to the Hugging Face Hub rather than committed to normal Git history.
