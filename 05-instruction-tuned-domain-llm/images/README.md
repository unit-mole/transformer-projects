# Images

Add real screenshots after local testing and deployment. Recommended file:

- `demo_screenshot.png` — full Gradio interface showing a successful adapter-backed response and inference metadata.

Do not add a fabricated screenshot that implies a model or evaluation run that has not occurred.
