{
  "name": "08-image-classification-vision-transformer",
  "version": "1.0.0",
  "private": true,
  "description": "Static browser-based Vision Transformer image classifier for GitHub Pages",
  "scripts": {
    "serve": "npx http-server web -p 8000 -c-1",
    "check": "node --check web/app.js && node --check web/inference.js && node --check web/attention.js && node --check web/image_preprocessing.js"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
