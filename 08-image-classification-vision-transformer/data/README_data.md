# Data directory

`class_names.json` contains the CIFAR-10 class order used by the training framework. The full dataset is downloaded locally by `torchvision` into `data/raw/`, which is ignored by Git.

`sample_images/` is reserved for safe, appropriately licensed or synthetic images. Do not add private, confidential, proprietary, or personally identifying images. Remove EXIF metadata where practical.
