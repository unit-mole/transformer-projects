Prompt for project 4 : 

(I am giving you my built project also, so you can make use of the same and can built something new also if it is not fulfilling what I eventually want from my project end of the day.)

=================================

Great, now we have started working on **Transformer Models**.

There are in total **10 Transformer projects** that I will be building in this portfolio series.

Below is the complete list:

```text id="b32n0x"
01-abstractive-text-summarization-transformer
02-neural-machine-translation-transformer
03-cross-encoder-bi-encoder-ranking-system
04-long-document-question-answering-longformer
05-instruction-tuned-domain-llm
06-multimodal-visual-question-answering-transformer
07-document-semantic-search-sentence-bert
08-image-classification-vision-transformer
09-vision-language-image-text-retrieval-clip
10-ai-portfolio-rag-assistant
```

Previously, I completed my:

```text id="z22akw"
ANN / Deep Learning projects
Simple RNN projects
LSTM projects
Bi-Directional LSTM projects
CNN / Computer Vision projects
```

Now I am working on my new portfolio series based on:

```text id="ougx7z"
Transformer Models / Attention-Based Deep Learning / Generative AI / NLP / Long-Document Understanding
```

I currently work as a **Quality Data Scientist**, and I want to improve my GitHub profile so that it looks professional for future roles in:

* Data Science
* Machine Learning
* AI / Applied AI
* Generative AI
* Natural Language Processing
* Transformer Models
* Long-Document Question Answering
* Document AI
* Information Extraction
* Search and Retrieval
* RAG Systems
* LLM Applications
* Data Analytics
* Quality Analytics
* Analytics Engineering

---

# Main Repository Context

Please create or assume a GitHub repository for this Transformer series named:

```text id="c1d97z"
transformer-models-projects
```

If I provide a different repository name later, use that exact repository name instead.

This repository should be similar in quality and structure to my previous repositories:

```text id="m1aq2n"
ann-deep-learning-projects
simple-rnn-projects
lstm-projects
bi-directional-lstm-projects
cnn-projects
transformer-models-projects
```

The goal is to build a professional **Transformer Models portfolio repository** containing advanced NLP, translation, ranking, long-document QA, instruction tuning, multimodal AI, semantic search, Vision Transformer, CLIP retrieval, and RAG projects with:

* clean code,
* modular Python files,
* professional README files,
* model evaluation,
* saved model artifacts or Hugging Face model references,
* Hugging Face demos,
* Gradio apps,
* deployment instructions,
* GitHub Actions workflow files,
* live demo placeholders,
* recruiter-friendly presentation,
* comparison with previous model approaches where relevant.

---

# Current Project

Here we are focusing on the fourth Transformer project:

```text id="k4kz1x"
04-long-document-question-answering-longformer
```

This project should demonstrate:

```text id="dd4hsh"
Long-Document Question Answering using Longformer or BigBird
```

The final project should be placed under:

```text id="19roel"
transformer-models-projects/
│
└── 04-long-document-question-answering-longformer/
```

---

# Project Pattern Requirement

This project must strictly follow the below pattern:

```text id="b42kmu"
Project Number:
04

Project Name:
04-long-document-question-answering-longformer

Application:
Allow users to upload or select a long document and ask questions.

Required Output:
Answer
Confidence score
Supporting paragraph
Highlighted evidence

Model:
Longformer or BigBird

Dataset:
Qasper, long-document QA subset, or selected reports

Evaluation Metrics:
Exact Match
Token-level F1
Evidence recall
Context-length analysis

Deployment:
Hugging Face
```

Do not ignore this pattern.

The project must be built depending on the final deployment platform mentioned above.

Since the deployment platform is:

```text id="wdnr3h"
Hugging Face
```

the entire project should be designed as a **Hugging Face Spaces + Gradio deployment-ready project** from the beginning.

The app should not be created in a way that only works locally. It should be structured so that I can deploy it on **Hugging Face Spaces** at the end.

Final deployment must be done on **Hugging Face Spaces using Gradio**, and the final README should include a live Hugging Face demo link placeholder.

---

# Current Task

Please take the attached Long-Document Question Answering files and convert them into a professional portfolio project.

I want this project to be:

1. GitHub-ready
2. Recruiter-friendly
3. Technically strong
4. Easy to understand
5. Easy to run locally
6. Suitable for hosting as a live Hugging Face Spaces demo
7. Built with Gradio for the interactive long-document QA demo
8. Cleanly structured inside the `transformer-models-projects` repository
9. Deployment-ready using Hugging Face Spaces
10. Consistent with my previous ANN, Simple RNN, LSTM, BiLSTM, CNN, and Transformer portfolio structure
11. Properly documented with README, requirements, scripts, outputs, tests, model card, deployment files, and GitHub workflow YAML
12. Designed specifically for the long-document QA pattern mentioned above

---

# Existing Models / Files Requirement

I will also provide the models, notebooks, scripts, or earlier code files that I have already built with respect to this particular project.

Please inspect those files carefully and reuse, improve, or restructure them wherever helpful.

I may provide:

```text id="x91051"
previous long-document QA code
Longformer model files
BigBird model files
Hugging Face QA pipeline files
document chunking scripts
PDF/text document parsing scripts
question-answering notebooks
dataset preprocessing files
evaluation scripts
Exact Match outputs
token-level F1 outputs
evidence recall outputs
context-length analysis outputs
Gradio demo files
saved model artifacts
manual error analysis files
any other related files
```

Please use my actual provided files as the base version and convert the project into a complete GitHub-ready, Hugging Face-ready, Gradio-ready, and portfolio-ready project.

Do not give generic advice only.

Do not create a random QA project without checking the files I provide.

---

# Important Requirement

Please inspect the attached project files before making recommendations or generating updated code.

First identify what the current project is doing:

```text id="n8jihh"
long-document question answering
extractive question answering
Longformer QA
BigBird QA
scientific document QA
report question answering
PDF question answering
supporting evidence extraction
highlighted answer span
context window analysis
document chunking
evidence paragraph retrieval
any other long-document QA task
```

Then improve the project according to the actual project objective.

If the current code is basic, improve it into a complete end-to-end **Long-Document Question Answering using Longformer or BigBird** project.

---

# Expected Root Repository Structure

The root repository should follow this structure:

```text id="nh3n8u"
transformer-models-projects/
│
├── .github/
│   └── workflows/
│       ├── 01-abstractive-text-summarization-transformer.yml
│       ├── 02-neural-machine-translation-transformer.yml
│       ├── 03-cross-encoder-bi-encoder-ranking-system.yml
│       └── 04-long-document-question-answering-longformer.yml
│
├── 01-abstractive-text-summarization-transformer/
├── 02-neural-machine-translation-transformer/
├── 03-cross-encoder-bi-encoder-ranking-system/
├── 04-long-document-question-answering-longformer/
├── 05-instruction-tuned-domain-llm/
├── 06-multimodal-visual-question-answering-transformer/
├── 07-document-semantic-search-sentence-bert/
├── 08-image-classification-vision-transformer/
├── 09-vision-language-image-text-retrieval-clip/
├── 10-ai-portfolio-rag-assistant/
│
├── .gitignore
├── LICENSE
└── README.md
```

For now, create and fully complete only:

```text id="fftk4x"
04-long-document-question-answering-longformer/
```

Also create or update the root-level files needed for the repository.

---

# Expected Project Folder Structure

Please organize the fourth Transformer project in a clean GitHub-ready structure.

Recommended structure:

```text id="rpw0mp"
transformer-models-projects/
│
└── 04-long-document-question-answering-longformer/
    │
    ├── README.md
    ├── app.py
    ├── gradio_app.py
    ├── data/
    │   ├── sample_documents/
    │   ├── sample_questions.csv
    │   ├── sample_qa_pairs.csv
    │   └── README_data.md
    ├── notebooks/
    │   ├── long_document_question_answering_longformer.ipynb
    │   └── evidence_recall_context_length_analysis.ipynb
    ├── src/
    │   ├── data_preprocessing.py
    │   ├── document_loader.py
    │   ├── text_preprocessing.py
    │   ├── document_chunking.py
    │   ├── qa_model.py
    │   ├── answer_extraction.py
    │   ├── evidence_highlighting.py
    │   ├── confidence_scoring.py
    │   ├── model_evaluation.py
    │   ├── context_length_analysis.py
    │   ├── inference_pipeline.py
    │   └── visualization.py
    ├── models/
    │   ├── long_document_qa_model/
    │   ├── tokenizer/
    │   └── model_metadata.json
    ├── outputs/
    │   ├── document_length_distribution.png
    │   ├── question_length_distribution.png
    │   ├── context_length_analysis.png
    │   ├── exact_match_results.json
    │   ├── token_f1_results.json
    │   ├── evidence_recall_results.json
    │   ├── qa_examples.csv
    │   ├── highlighted_evidence_examples.html
    │   ├── manual_error_analysis.md
    │   └── model_metrics.json
    ├── images/
    │   └── demo_screenshot.png
    ├── tests/
    │   ├── test_document_loader.py
    │   ├── test_document_chunking.py
    │   ├── test_answer_extraction.py
    │   ├── test_evidence_highlighting.py
    │   └── test_inference_pipeline.py
    ├── scripts/
    │   ├── preprocess_documents.py
    │   ├── evaluate_model.py
    │   ├── run_context_analysis.py
    │   └── run_gradio.py
    ├── requirements.txt
    ├── README_HUGGINGFACE.md
    ├── MODEL_CARD.md
    ├── Dockerfile
    ├── .dockerignore
    └── .gitignore
```

You can modify this structure if a better structure is needed, but keep it clean, professional, and consistent with my earlier portfolio projects.

---

# Hugging Face Spaces Structure Requirement

Since this project will be hosted on Hugging Face Spaces using Gradio, please make sure the project can run with this setup:

```text id="zqvmix"
04-long-document-question-answering-longformer/
│
├── app.py
├── requirements.txt
├── README.md
├── MODEL_CARD.md
├── src/
│   └── required Python files
├── data/
│   ├── sample_documents/
│   └── sample_questions.csv
└── models/
    └── optional local model artifacts
```

The Hugging Face Space should be able to run from:

```text id="e6eirn"
app.py
```

The app should not require training during startup.

The app should load a pretrained or fine-tuned Longformer / BigBird model and perform inference directly.

Final deployment must be done on **Hugging Face Spaces using Gradio**, and the final README should include a live Hugging Face demo link placeholder.

---

# GitHub Actions Requirement

Please create a GitHub Actions workflow YAML file for this project.

The YAML file should be saved here:

```text id="qz41ec"
.github/workflows/04-long-document-question-answering-longformer.yml
```

The workflow should:

1. Run on push and pull request.
2. Set up Python.
3. Install dependencies from the project requirements file.
4. Run basic tests if tests are available.
5. Validate that the Gradio app imports correctly.
6. Validate that the inference pipeline imports correctly.
7. Validate that document loading and evidence highlighting modules import correctly.
8. Avoid retraining the full Longformer / BigBird model inside GitHub Actions.
9. Avoid downloading very large datasets inside GitHub Actions.
10. Keep the workflow lightweight and suitable for a portfolio repository.

Recommended workflow name:

```text id="kio7v5"
04 Long Document Question Answering Longformer CI
```

---

# Project Objective

The project should clearly explain the NLP / Document AI problem:

Long-document question answering is an NLP task where the model answers questions using information from long documents such as research papers, reports, policies, manuals, case reports, or business documents.

This project should allow users to:

```text id="q7qmfa"
upload or select a long document
ask a question
receive an answer
receive a confidence score
view the supporting paragraph
view highlighted evidence
```

The project should clearly explain:

* how long documents are loaded,
* how text is extracted from uploaded files,
* how long documents are cleaned,
* how documents are chunked if needed,
* how Longformer or BigBird handles long context,
* how the question is passed to the model,
* how the answer span is extracted,
* how the supporting paragraph is selected,
* how evidence is highlighted,
* how confidence information is estimated,
* how Exact Match, token-level F1, evidence recall, and context-length analysis are measured,
* how the final demo is deployed on Hugging Face Spaces.

---

# Model Requirement

Use a suitable long-document Transformer QA model.

Preferred models:

```text id="sxov7q"
Longformer
BigBird
```

Recommended model options:

```text id="ei2ny4"
allenai/longformer-base-4096
allenai/longformer-large-4096
valhalla/longformer-base-4096-finetuned-squadv1
google/bigbird-roberta-base
google/bigbird-roberta-large
```

Please choose the best model based on:

* long-context support,
* Hugging Face Spaces feasibility,
* model size,
* inference speed,
* answer quality,
* ease of deployment.

For portfolio purposes, prefer:

```text id="bi3hhw"
Longformer-based QA model for long context support
or
BigBird if the attached files already use it well
```

The project should clearly document why the selected model was chosen.

If the chosen base model is not directly fine-tuned for QA, explain whether you fine-tuned it or used a QA-compatible checkpoint.

Do not falsely claim fine-tuning if it was not done.

---

# Dataset Requirement

Use or support a long-document QA dataset such as:

```text id="iiivbn"
Qasper
long-document QA subset
selected reports
custom report-question-answer dataset
sample long-document QA dataset
```

Please inspect the attached files and identify the actual dataset structure.

The dataset may include columns such as:

```text id="x2elno"
document_id
title
document
paragraph
context
question
answer
answer_text
answer_start
evidence
supporting_paragraph
supporting_sentence
confidence
```

Use the actual columns, folders, filenames, and label structure available in the attached dataset.

Clearly identify:

```text id="dyerhi"
Dataset name
Dataset source
Number of documents
Number of questions
Document column
Question column
Answer column
Evidence column
Average document length
Maximum document length
Train / validation / test split
Whether the dataset is Qasper, reports, public, synthetic, or sample-only
```

If the dataset is large, copyrighted, restricted, or not practical to push to GitHub, use a small safe sample dataset and document the full dataset source.

Do not push a large full dataset to GitHub.

---

# Document Upload Requirement

The Gradio app must allow users to upload a document.

Supported formats should include:

```text id="mb0qv5"
.txt
.md
.csv text column if feasible
.pdf if lightweight parsing is feasible
```

For PDF support:

```text id="re5gc0"
Use lightweight PDF extraction if possible.
If PDF extraction adds deployment complexity, include it as optional.
Make sure the app works reliably with .txt and sample documents.
```

The app should also include preloaded sample documents so users can test it quickly without uploading files.

---

# Document Loading Requirement

Please include a robust document loading module.

The module should handle:

```text id="skji7n"
plain text documents
markdown documents
optional PDF extraction
sample document selection
uploaded document text extraction
empty file handling
unsupported file handling
very long document handling
```

Do not let the app crash if a document is empty, too long, unsupported, or poorly formatted.

---

# Long-Context Handling Requirement

Since this project is for long-document QA, please clearly handle long context.

The project should support:

```text id="lfrn7o"
model max context length
token-based truncation
sliding windows if needed
chunking strategy
answer aggregation across chunks if needed
supporting paragraph extraction
context-length analysis
```

The README should explain:

```text id="my2ble"
Standard Transformers have limited context windows.
Longformer and BigBird are designed to handle longer contexts more efficiently.
For extremely long documents, chunking or sliding-window processing may still be required.
```

---

# Evidence Highlighting Requirement

The project must return highlighted evidence.

The output should include:

```text id="cvsl7w"
answer text
confidence score
supporting paragraph
highlighted evidence
document section / paragraph index if available
```

The highlighting should:

```text id="jb08vl"
highlight the answer span inside the supporting paragraph if available
show surrounding context
avoid inventing evidence if the answer span cannot be located
return a clear message if evidence cannot be confidently identified
```

Do not fake evidence.

If evidence extraction is uncertain, say so clearly.

---

# Confidence Score Requirement

The application must show confidence information.

Important note:

QA confidence scores from Transformer models are not always calibrated probabilities.

Therefore, please label it honestly as:

```text id="ab5bch"
confidence proxy
```

or:

```text id="wpr60x"
model confidence estimate
```

Possible confidence methods:

```text id="mkf6vi"
start/end logit score combination
softmax probability over answer span
normalized answer score
fallback confidence heuristic if the pipeline does not expose reliable scores
```

The app should clearly explain:

```text id="s6hhb8"
The confidence score is a model-based proxy and should not be treated as a guarantee that the answer is correct.
```

---

# Evaluation Requirement

This long-document QA project should include proper evaluation metrics.

Please include:

```text id="a5zn44"
Exact Match
Token-level F1
Evidence recall
Context-length analysis
Answer latency if feasible
Example question-answer results
Manual error analysis
```

The README should explain:

* Exact Match checks whether the predicted answer exactly matches the reference answer.
* Token-level F1 gives partial credit based on overlapping answer tokens.
* Evidence recall checks whether the model selected the correct supporting paragraph or evidence text.
* Context-length analysis shows how answer quality changes as document length increases.
* Long-document QA requires both answer correctness and evidence grounding.

Do not rely only on training loss or accuracy.

---

# Evidence Recall Requirement

Please include an evidence recall analysis.

The project should show:

```text id="jecwv6"
question
reference answer
predicted answer
reference evidence paragraph
predicted supporting paragraph
whether evidence was recovered
evidence recall score
qualitative observation
```

Recommended comparison table:

```text id="c4b7kc"
Question
Predicted Answer
Reference Answer
Predicted Evidence
Reference Evidence
Exact Match
Token F1
Evidence Recovered
Observation
```

Do not invent metrics.

Use actual values only after running evaluation.

---

# Context-Length Analysis Requirement

This is an important part of the project.

The project must analyze how context length affects QA performance.

Please include analysis such as:

```text id="oxvu43"
short documents
medium-length documents
long documents
very long documents
model truncation behavior
chunking behavior
latency by context length
Exact Match by context length
F1 by context length
evidence recall by context length
```

Recommended output:

```text id="t9q1jg"
Context Length Bucket    Exact Match    Token F1    Evidence Recall    Avg Latency
0-512 tokens             <value>        <value>     <value>            <value>
513-1024 tokens           <value>        <value>     <value>            <value>
1025-2048 tokens          <value>        <value>     <value>            <value>
2049-4096 tokens          <value>        <value>     <value>            <value>
```

Do not invent metrics.

Use actual values only after running evaluation.

---

# Manual Error Analysis Requirement

Please include a manual error analysis section.

The project should show examples of:

```text id="qg48ro"
good answers
weak answers
wrong answer span
missing answer
unsupported answer
low-confidence answer
answer from wrong paragraph
evidence not recovered
truncation-related failures
long-context failures
ambiguous questions
```

Use actual examples from the model results.

Do not overstate model capability.

---

# Baseline Comparison Requirement

Please include a baseline comparison if feasible.

Compare the long-document QA model against simple baselines such as:

```text id="zsf1v2"
keyword paragraph retrieval plus short QA model
BM25 paragraph retrieval plus QA model
standard BERT QA with truncated context
Longformer / BigBird long-document QA
```

Recommended comparison table:

```text id="a5qavt"
Model / Approach                    Exact Match    Token F1    Evidence Recall    Avg Latency
Keyword + Short QA                  <value>        <value>     <value>            <value>
BM25 + Short QA                     <value>        <value>     <value>            <value>
BERT QA Truncated Context           <value>        <value>     <value>            <value>
Longformer / BigBird QA             <value>        <value>     <value>            <value>
```

If full baseline implementation is not possible, explain the recommended baseline approach in the README and notebook.

Do not invent metrics.

Use actual values only after running evaluation.

---

# Gradio Demo Requirement

I want this project to be hosted as a Hugging Face Spaces demo so that someone can click a link and interact with it.

Please create a clean and professional **Gradio app** for this project.

The Gradio app should allow users to:

1. Upload a long document.
2. Select a preloaded sample document.
3. Paste long text manually.
4. Ask a question about the document.
5. Generate an answer.
6. View confidence proxy.
7. View supporting paragraph.
8. View highlighted evidence.
9. View document length and context length used.
10. View answer latency if feasible.
11. View model details and limitations.

The app should show:

* project title,
* responsible-use disclaimer,
* short project explanation,
* document upload section,
* sample document selector,
* manual document text area,
* question input box,
* answer output,
* confidence proxy,
* supporting paragraph,
* highlighted evidence,
* document/context length information,
* model details,
* evaluation metrics,
* context-length analysis notes,
* manual error analysis notes,
* limitations section,
* GitHub link placeholder,
* Hugging Face model link placeholder if applicable.

The app should be suitable for hosting on:

```text id="hhffar"
Hugging Face Spaces
```

The deployed app should load pretrained or fine-tuned model artifacts and should not require training during app startup.

---

# Gradio Design Requirement

Please make the Gradio app polished and portfolio-friendly.

The app should include:

```text id="uzep11"
Project title
Short project explanation
Responsible-use disclaimer
Document input tab
Sample document selector
Manual text input
File upload input
Question input box
Ask Question button
Answer output
Confidence proxy metric
Supporting paragraph output
Highlighted evidence output
Document length metric
Context length metric
Latency metric
Model details section
Evaluation metrics section
Context-length analysis section
Manual error analysis section
Limitations section
GitHub link placeholder
```

Use a clean Gradio interface such as:

```text id="petbny"
gr.Blocks()
gr.Markdown()
gr.Tabs()
gr.Tab()
gr.File()
gr.Textbox()
gr.Dropdown()
gr.Button()
gr.HighlightedText()
gr.HTML()
gr.JSON()
gr.Number()
```

The app should be simple, clean, and easy for recruiters or technical reviewers to test.

Do not make the demo depend on GPU access.

---

# Responsible Use Requirement

Because this project answers questions from user-provided documents, the app and README must clearly include a responsible-use note.

The app and README should clearly state:

```text id="dzp9cl"
This project is for educational and portfolio demonstration purposes only.
The model may return incomplete, incorrect, unsupported, or misleading answers.
The confidence score is only a model-based proxy and does not guarantee answer correctness.
Highlighted evidence may be incomplete or may not fully support the generated answer.
Do not use generated answers as the sole basis for legal, medical, financial, safety-critical, academic, official, or business-critical decisions.
Do not upload private, confidential, sensitive, copyrighted, proprietary, or personally identifiable documents into the public demo app.
All answers and evidence should be reviewed by a human before real-world use.
```

This disclaimer should appear clearly in:

1. `README.md`
2. Hugging Face / Gradio app interface
3. Notebook introduction section

---

# Hugging Face Spaces Deployment Requirement

Please prepare this project specifically for Hugging Face Spaces.

The Hugging Face Space should include:

```text id="fb6vkl"
app.py
requirements.txt
README.md
MODEL_CARD.md
src/
data/sample_documents/
data/sample_questions.csv
```

If models are loaded directly from Hugging Face Hub, document the model names clearly.

If fine-tuned artifacts are saved locally, explain how to upload them.

The `README.md` or Space card should include:

```text id="wnue30"
title
emoji
colorFrom
colorTo
sdk: gradio
sdk_version
app_file: app.py
pinned: false
license
```

Example Space metadata:

```yaml id="e8bqdk"
---
title: Long Document Question Answering Longformer
emoji: 📄
colorFrom: blue
colorTo: green
sdk: gradio
sdk_version: 5.0.0
app_file: app.py
pinned: false
license: mit
---
```

Please create deployment-ready files for Hugging Face Spaces.

---

# Hugging Face Model / File Size Requirement

If the model files are too large for normal GitHub upload, please explain how to handle them.

Options:

```text id="xt5xch"
Use Hugging Face Hub model repository
Use Git LFS
Upload model to Hugging Face Space repository
Use pretrained models directly from Hugging Face Hub
Use a smaller QA-compatible model for demo stability
Use sample documents for the public demo
```

The demo should still work reliably.

Do not require users to train models when opening the demo.

---

# GitHub README Requirement

Create a complete professional project-level `README.md`.

The README should include:

1. Project title
2. Responsible-use disclaimer
3. NLP / long-document QA problem
4. Project objective
5. Strict project pattern section
6. Dataset description
7. Model selection: Longformer or BigBird
8. Tools and technologies used
9. Project workflow
10. Document loading workflow
11. Text preprocessing
12. Long-context handling
13. Document chunking or sliding-window strategy if used
14. Longformer / BigBird explanation
15. Answer extraction workflow
16. Confidence proxy explanation
17. Supporting paragraph extraction
18. Evidence highlighting explanation
19. Inference pipeline
20. Baseline comparison if feasible
21. Evaluation metrics
22. Exact Match, token-level F1, and evidence recall explanation
23. Context-length analysis
24. Results and QA examples
25. Manual error analysis
26. Error analysis and limitations
27. Hugging Face Spaces demo link placeholder
28. Hugging Face model links if applicable
29. Screenshots section
30. How to run locally
31. How to deploy on Hugging Face Spaces
32. Folder structure
33. Future improvements
34. Skills demonstrated

The README should be written in a recruiter-friendly and technical style.

It should make the project look strong on GitHub and clearly show that this is a **Long-Document Question Answering Transformer project deployed using Hugging Face Spaces**.

---

# Hugging Face README / Space Card Requirement

Create a separate Hugging Face Space README section or file if needed.

It should include:

```text id="r90vpn"
Project title
Short description
Responsible-use disclaimer
How to use the demo
Model details
Input format
Output format
Document upload
Answer
Confidence proxy
Supporting paragraph
Highlighted evidence
Evaluation metrics
Context-length analysis
Manual error analysis
Limitations
GitHub repository link
Portfolio note
```

---

# Model Card Requirement

Create a professional `MODEL_CARD.md`.

It should include:

```text id="q23cgy"
Model name
Base model
Task
Dataset
Intended use
Not intended use
Training or fine-tuning details if applicable
Evaluation metrics
Exact Match
Token-level F1
Evidence recall
Context-length analysis
Limitations
Bias and risk notes
Responsible-use note
Inference examples
Deployment details
```

---

# Local Run Instructions

Please provide step-by-step local run instructions.

Example:

```text id="vby50j"
1. Download the repository.
2. Open terminal inside transformer-models-projects.
3. Navigate to 04-long-document-question-answering-longformer.
4. Create a virtual environment.
5. Install dependencies using pip install -r requirements.txt.
6. Run document preprocessing using python scripts/preprocess_documents.py.
7. Run evaluation using python scripts/evaluate_model.py.
8. Run context-length analysis using python scripts/run_context_analysis.py.
9. Run the Gradio app locally using python app.py.
10. Open the local Gradio URL in the browser.
```

---

# Hugging Face Deployment Instructions

Please provide step-by-step Hugging Face Spaces deployment instructions.

Explain:

1. How to create a new Hugging Face Space.
2. Which SDK to select.
3. What files are required.
4. What should be inside `requirements.txt`.
5. Where `app.py` should be placed.
6. How to load models from Hugging Face Hub.
7. How to upload fine-tuned model artifacts if needed.
8. How to use Git LFS if model files are large.
9. How to test document upload and QA output.
10. What final demo link I can share.
11. How to add the Hugging Face demo link to the GitHub README.

---

# Code File Requirements

Please provide complete updated code files, not partial snippets.

Generate all necessary files such as:

```text id="vt7ysn"
README.md
README_HUGGINGFACE.md
MODEL_CARD.md
requirements.txt
.gitignore
Dockerfile
.dockerignore
app.py
gradio_app.py
src/data_preprocessing.py
src/document_loader.py
src/text_preprocessing.py
src/document_chunking.py
src/qa_model.py
src/answer_extraction.py
src/evidence_highlighting.py
src/confidence_scoring.py
src/model_evaluation.py
src/context_length_analysis.py
src/inference_pipeline.py
src/visualization.py
scripts/preprocess_documents.py
scripts/evaluate_model.py
scripts/run_context_analysis.py
scripts/run_gradio.py
tests/test_document_loader.py
tests/test_document_chunking.py
tests/test_answer_extraction.py
tests/test_evidence_highlighting.py
tests/test_inference_pipeline.py
notebooks/long_document_question_answering_longformer.ipynb
notebooks/evidence_recall_context_length_analysis.ipynb
.github/workflows/04-long-document-question-answering-longformer.yml
```

If some files are not needed, explain why.

If model files need to be generated after fine-tuning, explain where they will be saved.

---

# Model / Artifact File Requirement

The project should save all required model artifacts or configuration files for inference and demo use:

```text id="n9dnbl"
Selected Longformer or BigBird model name
Tokenizer name or path
Fine-tuned model path if applicable
Maximum context length
Chunking strategy
Sliding-window configuration if used
Answer extraction configuration
Confidence scoring method
Evidence highlighting method
Dataset information
Preprocessing configuration
Evaluation metrics
Exact Match results
Token-level F1 results
Evidence recall results
Context-length analysis results
Manual error analysis results
Responsible-use disclaimer
```

The Hugging Face app should load these artifacts directly and should not require training during app startup.

---

# Data Safety / GitHub Requirement

If the document dataset is large, copyrighted, restricted, proprietary, or not allowed for redistribution, please create a safe approach.

For example:

* keep full dataset out of GitHub if needed,
* include only small sample documents,
* use public dataset loading scripts,
* add `data/README_data.md` explaining the dataset source and usage,
* add `.gitignore` to prevent large/generated files from being uploaded,
* do not upload private documents,
* do not upload confidential company reports,
* do not upload proprietary technical manuals,
* do not upload copyrighted full reports unless redistribution is allowed,
* do not upload personally identifiable documents,
* use synthetic or public sample documents for the demo if needed.

Do not assume that the full dataset should be pushed to GitHub.

---

# Portfolio Positioning

Please also explain how I should present this project in my GitHub portfolio.

Tell me:

1. How to describe this project in one line.
2. How to describe it in my GitHub pinned repository section.
3. What skills this project demonstrates.
4. What screenshots I should include.
5. What output files I should save.
6. How this project supports my transition from Quality Data Scientist to Data Science / ML / AI roles.

Emphasize that this project demonstrates:

* Transformer modeling,
* long-document question answering,
* Longformer / BigBird usage,
* document loading,
* long-context handling,
* evidence extraction,
* evidence highlighting,
* confidence proxy scoring,
* Exact Match evaluation,
* token-level F1 evaluation,
* evidence recall,
* context-length analysis,
* Hugging Face Spaces deployment,
* Gradio app development,
* professional portfolio-ready ML project structure.

Also connect the project naturally to my current background as a Quality Data Scientist because long-document QA is relevant to searching long quality reports, answering questions from GCS case histories, extracting evidence from root-cause documents, reviewing SOPs, technical manuals, CAPA documents, complaint investigations, and automated quality knowledge-base workflows.

---

# Main Repository README Update Requirement

Since this is the fourth project inside the `transformer-models-projects` repository, please also create or update the main repository README.

The root README should include:

1. Repository title:

```text id="mjzo7h"
transformer-models-projects
```

2. Short professional description:

```text id="k8f08i"
A professional Transformer Models portfolio featuring abstractive summarization, neural machine translation, two-stage search ranking, long-document QA, instruction tuning, multimodal VQA, semantic search, Vision Transformers, CLIP retrieval, and RAG assistants.
```

3. My career positioning as a Quality Data Scientist building an advanced Transformer and Generative AI portfolio.

4. List of all planned Transformer projects:

```text id="wr2fgz"
01-abstractive-text-summarization-transformer
02-neural-machine-translation-transformer
03-cross-encoder-bi-encoder-ranking-system
04-long-document-question-answering-longformer
05-instruction-tuned-domain-llm
06-multimodal-visual-question-answering-transformer
07-document-semantic-search-sentence-bert
08-image-classification-vision-transformer
09-vision-language-image-text-retrieval-clip
10-ai-portfolio-rag-assistant
```

5. Completed projects section showing:

```text id="hrnrf5"
01-abstractive-text-summarization-transformer
02-neural-machine-translation-transformer
03-cross-encoder-bi-encoder-ranking-system
04-long-document-question-answering-longformer
```

6. Live demo link placeholder for Hugging Face Spaces.

7. Model card link placeholder if applicable.

8. Tech stack:

```text id="jseac0"
Python
PyTorch
Hugging Face Transformers
Hugging Face Datasets
Longformer
BigBird
Gradio
Hugging Face Spaces
pandas
NumPy
scikit-learn
Matplotlib
GitHub Actions
Docker if used
```

9. How the repository is organized.
10. Skills demonstrated.
11. Future roadmap.
12. Setup instructions.
13. Contribution / usage note if useful.

The README should be suitable for GitHub recruiters and technical reviewers.

---

# GitHub Repository Description and Topics

Please suggest a strong GitHub repository description for:

```text id="eykqan"
transformer-models-projects
```

Suggested description:

```text id="a7j7tx"
End-to-end Transformer Models portfolio featuring summarization, English-Hindi translation, two-stage search ranking, long-document QA, instruction tuning, multimodal VQA, semantic search, Vision Transformers, CLIP retrieval, RAG, Hugging Face models, and deployment-ready demos.
```

Please also suggest GitHub topics such as:

```text id="es6vk3"
transformers
huggingface
longformer
bigbird
long-document-qa
question-answering
document-ai
evidence-extraction
nlp
gradio
huggingface-spaces
machine-learning
deep-learning
data-science
portfolio-projects
```

---

# Final Output Required

Please provide:

1. Complete project overview.
2. Recommended `transformer-models-projects` repository setup/update.
3. Recommended GitHub repository description and topics.
4. Root repository README content/update.
5. Recommended project folder structure.
6. Updated / cleaned Python code files.
7. Gradio demo app code.
8. Hugging Face Spaces-ready `app.py`.
9. Document upload and sample document workflow.
10. Long-context handling and chunking module.
11. Answer extraction module.
12. Confidence proxy scoring framework.
13. Supporting paragraph and highlighted evidence framework.
14. Evaluation scripts for Exact Match, token-level F1, evidence recall, and context-length analysis.
15. Manual error analysis framework.
16. GitHub Actions workflow YAML.
17. `requirements.txt`.
18. `.gitignore`.
19. `Dockerfile` if useful.
20. `.dockerignore` if Docker is included.
21. Professional project-level `README.md`.
22. Hugging Face Spaces README / Space card content.
23. `MODEL_CARD.md`.
24. Hugging Face deployment guide.
25. Local run guide.
26. Suggestions for screenshots and visuals.
27. Recruiter-friendly project description.
28. Explanation of changes made to the original code.
29. Tests or validation scripts if feasible.
30. All coding files in a downloadable format so I can run the project locally on my system.

Please generate the complete GitHub-ready, Hugging Face Spaces-ready, Gradio-ready, and portfolio-ready version of the **04-long-document-question-answering-longformer** project now.
