# Development and CI-only dependencies.
# Install together with requirements.txt so pip resolves the complete
# dependency graph once:
#   python -m pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.3.0,<10.0.0
pytest-cov>=5.0.0,<7.0.0
ruff>=0.8.0,<1.0.0
nbformat>=5.10.0,<6.0.0
openpyxl>=3.1.5,<4.0.0
