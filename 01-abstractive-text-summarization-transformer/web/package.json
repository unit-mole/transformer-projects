{
  "name": "project-01-abstractive-summarization-static-space",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "description": "Free Static Space with reliable full-precision browser summarization using a fine-tuned T5-small ONNX checkpoint.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test tests/*.test.js",
    "check": "npm test && npm run build"
  },
  "dependencies": {
    "@huggingface/transformers": "4.2.0"
  },
  "devDependencies": {
    "vite": "6.4.3"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
