# Lightweight GitHub Actions dependencies.
# The application lazy-loads PyTorch and Transformers only when inference is
# requested, so CI can validate tests and imports without downloading a large
# model runtime or model weights.
gradio==5.49.1
huggingface-hub==0.36.2
pandas>=2.2.0,<3.0.0
numpy>=1.26.0,<3.0.0
scikit-learn>=1.5.0,<2.0.0
PyYAML>=6.0.0,<7.0.0
pytest>=8.3.0,<10.0.0
