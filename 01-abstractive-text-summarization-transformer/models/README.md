# Model Artifacts

No large model weights are committed.

- Default inference downloads `sshleifer/distilbart-cnn-12-6` from the Hugging Face Hub.
- Optional fine-tuning writes weights to `transformer_summarization_model/`.
- Tokenizer artifacts are written to `tokenizer/`.
- Push large artifacts to a dedicated Hugging Face model repository or use Git LFS.
