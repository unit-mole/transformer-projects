# Model artifacts

The default demo downloads pretrained MarianMT weights from the Hugging Face Hub and does not store them here.

Optional fine-tuned directories:

```text
models/fine_tuned_en_hi/
models/fine_tuned_hi_en/
```

Do not commit large model files as ordinary Git blobs. Push them to a dedicated Hugging Face model repository or use Git LFS.
